const express=require("express"),path=require("path");const app=express(),PORT=process.env.PORT||3000,KEY=process.env.FUGLE_API_KEY,SYMS=["2327","3037","3706","3006","2313","6147"];app.use(express.static(path.join(__dirname,"public")));async function quote(symbol){if(!KEY)throw Error("尚未設定 FUGLE_API_KEY");let r=await fetch(`https://api.fugle.tw/marketdata/v1.0/stock/intraday/quote/${symbol}`,{headers:{"X-API-KEY":KEY}}),j=await r.json();if(!r.ok)throw Error(j.message||`Fugle HTTP ${r.status}`);return{symbol,price:Number(j.lastPrice??j.closePrice??j.lastTrade?.price),previousClose:Number(j.previousClose??j.referencePrice??0),high:Number(j.highPrice??0),low:Number(j.lowPrice??0),volume:Number(j.total?.tradeVolume??j.tradeVolume??0),lastSize:Number(j.lastTrade?.size??0),timestamp:j.lastTrade?.time?Date.parse(j.lastTrade.time):Date.now()}}app.get("/api/quotes",async(q,res)=>{try{res.json({data:(await Promise.all(SYMS.map(quote))).filter(x=>isFinite(x.price))})}catch(e){res.status(500).json({error:e.message})}});app.listen(PORT,()=>console.log(`Lisa V3 http://localhost:${PORT}`));